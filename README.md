"Recomendación de Libros"

Requisitos:

Python 3.6+

archivo data.json(Dataset en la misma carpeta)

No necesitas instalar nada adicional. El programa usa solo librerías incluidas en Python:
*json
*tkinter
*datetime